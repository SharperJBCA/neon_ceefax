import React, { JSX } from "react";

function TerminalOutput ({props}) {
    const outputList = 0 // props.outputs.map((o, key) => <div key={key}>{o}</div>)
    return <div>
    </div>
}

export default TerminalOutput;